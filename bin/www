#!/usr/bin/env node


var express = require('../app/server/express');

//run express server

var server = require('http').Server(express);
server.listen(express.get('port'), function() {
    console.info('Express server started at http://localhost:' + express.get('port'));
});